.model small
.stack 50
.data
tb1 db 'nhap vao ma sinh vien:$'
tb2 db 10,13,'dao nguoc ma sinh vien:$'
str db 50 dup('$')
.code 
     main proc
        mov ax,@data
        mov ds,ax
        
        mov ah,9
        lea dx,tb1
        int 21h
        
         mov ah,0ah
         lea dx,str
         int 21h
         
         mov ah,9
         lea dx,tb2
         int 21h
         
         mov cl,[str+1]
         lea si,[str +2]
         lap:
             push [si]
             inc si
             loop lap
             
          mov cl,[str +1]
          lap2:
               pop dx
               mov ah,2
               int 21h
               loop lap2
               
           mov ah,4ch
           int 21
        
       
    main endp
     end main
              
              